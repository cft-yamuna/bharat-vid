# Source videos (~500+ MB) are downloaded locally into bh_videos/ so the
# app runs fully offline. Several exceed GitHub's 50 MB warning threshold,
# so they stay out of git. To include them anyway, delete this rule and
# use Git LFS.
bh_videos/
Bharath Connect/

# OS cruft
Thumbs.db
desktop.ini
.DS_Store
